<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/editStyle.css">
</head>
<body>
    <div class="container">
        <h2>Edit Your Post</h2>
        <p class="post-id">Post ID: <span><%= post.id %></span></p>
        <p class="post-username">Username: <span>@<%= post.username %></span></p>
        <form method="post" action="/posts/<%= post.id %>?_method=PATCH">
            <textarea rows="10" name="content" placeholder="Edit your content..."><%= post.content %></textarea>
            <button type="submit">Update Post</button>
        </form> 
        <a href="/posts" class="back-button">Back to Posts</a>
    </div>
</body>
</html>
